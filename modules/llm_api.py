def call_mistral_api(messages):
    """
    Appelle le LLM Mistral/Ollama avec la liste de messages.
    À adapter selon ton backend réel (API REST, local, etc.)
    """
    # Exemple fictif (à remplacer par ton vrai appel)
    # response = requests.post("http://localhost:port/api", json={"messages": messages})
    # return response.json()
    return {"content": "Réponse fictive du LLM (à remplacer par l'appel réel)"}